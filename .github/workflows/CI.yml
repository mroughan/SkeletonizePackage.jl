name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Tests - Julia ${{ matrix.version }} - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        version:
          - '1.10'
          - '1.12'
        os:
          - ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
      - uses: julia-actions/cache@v2
      - name: Run package tests
        shell: julia --color=yes {0}
        env:
          SKELETONIZE_RUN_QUALITY_TESTS: "false"
        run: |
          using Pkg
          Pkg.activate(; temp=true)
          Pkg.add(PackageSpec(url="https://github.com/mroughan/IncCSV.jl"))
          Pkg.develop(PackageSpec(path=pwd()))
          Pkg.test("SkeletonizePackage"; coverage=true)
      - uses: julia-actions/julia-processcoverage@v1
      - uses: codecov/codecov-action@v4
        with:
          files: lcov.info
          flags: julia-${{ matrix.version }}
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
