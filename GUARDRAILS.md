The following file is a set of RULES that you MUST follow.

## General

 + Use US English spelling throughout, e.g., for variable and function names, and for documentation.
 + Never perform a large-scale deletion of files.
 + Never change a file outside of the folder being edited.
 + Don’t create any secrets.
 + Always look for a ARCHITECTURE.md and an AGENTS.md file and follow the ideas expressed in them. 
 + Use references for any important decision, either from the research literature, best practice documents or other online advice. In each case make clear where the advice came from and download the reference material such that it can be checked for veracity.


## Git
 + Never commit changes.
 + Never push changes.
 + Never make an irreversable change.

## General coding

 + Review code for repeated patterns that could be combined into helper functions.
 + Every time a change is introduced write a test to validate the idea behind the change.
 + Only alter ARCHITECTURE.md when specifically instructed to alter it.

## Julia language-specific coding

 + Use Julia style guidelines https://docs.julialang.org/en/v1/manual/style-guide/
 + Use standard Julia performance tips: https://docs.julialang.org/en/v1/manual/performance-tips/
 + Include docstrings for all exported types and methods.
 + Docstrings should include examples, with output values that can be tested.
 + Write code such that it will be easily acceptable to the Julia community.
 + Aim for high single-thread performance.
 + Make sure any Julia-focussed repository has a suitable set of elements in .gitignore

## Agents

 + Warn very clearly if these instructions are in danger of falling out of context.
 + Never alter this file.
 
 
 
 
 
 

